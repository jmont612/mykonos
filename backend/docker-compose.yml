services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: marketplace
      POSTGRES_PASSWORD: marketplace
      POSTGRES_MULTIPLE_DATABASES: marketplace_dev,marketplace_test
    ports:
      - "5433:5432"
    volumes:
      - marketplace_pg_data:/var/lib/postgresql/data
volumes:
  marketplace_pg_data:
